const routes = {
    landing: "/",
    home: "/home",
    login: "/login",
    register: "/register",
};

const apiRoutes = {
    getFriends: "/friend/fetchAll/",
    getUser: "/user/fetch/"
}

export { routes, apiRoutes };